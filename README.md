<div align="center">
<img width="1200" height="475" alt="GHBanner" src="https://github.com/user-attachments/assets/0aa67016-6eaf-458a-adb2-6e31a0763ed6" />
</div>

# 🎄 Grand Luxury Christmas Tree

一个使用 React + Three.js 构建的交互式 3D 圣诞树项目，支持手势控制和照片上传功能。

View your app in AI Studio: https://ai.studio/apps/drive/1eekx3QZG5NSxHvwAjrcVWYWS7-RN8fKC

## 📋 功能特性

- ✨ 精美的 3D 圣诞树渲染
- 🎨 支持自定义颜色和装饰
- 📸 上传照片装饰圣诞树
- 👋 手势控制（需要摄像头）
- 🖱️ 鼠标交互支持
- 📱 响应式设计

## 🚀 快速开始

### 环境要求

- **Node.js** >= 18.0.0（推荐使用 Node.js 18+）
- **npm** 或 **yarn**

### 本地开发

1. **安装依赖：**

   ```bash
   npm install
   ```

2. **配置环境变量：**

   创建 `.env.local` 文件，设置 Gemini API Key（如果使用 AI 功能）：

   ```env
   GEMINI_API_KEY=your_api_key_here
   ```

3. **启动开发服务器：**

   ```bash
   npm run dev
   ```

   访问 http://localhost:3000

4. **预览构建结果：**
   ```bash
   npm run preview
   ```

## 📦 构建项目

### 本地分发（推荐）

用于打包后直接发给别人使用，会自动移除 CORS 限制并包含启动脚本：

```bash
npm run build:local
```

构建完成后，`dist` 目录会包含：

- `index.html` - 主页面（已优化，可直接使用）
- `start-server.sh` - Mac/Linux 启动脚本
- `start-server.bat` - Windows 启动脚本
- `使用说明.txt` - 使用指南
- 所有静态资源文件

**使用方法：**

1. 将整个 `dist` 文件夹发给别人
2. 对方双击 `start-server.sh`（Mac/Linux）或 `start-server.bat`（Windows）
3. 在浏览器中访问 `http://localhost:8000`

### 线上部署

用于部署到服务器（GitHub Pages、Vercel、Netlify 等）：

```bash
npm run build:deploy
```

或使用默认构建命令：

```bash
npm run build
```

构建完成后，将 `dist` 目录的内容部署到你的服务器即可。

**注意：** 线上部署时不会有 CORS 问题，因为通过 HTTP/HTTPS 访问时浏览器会正常处理资源加载。

## 🌐 部署指南

### GitHub Pages

#### 方法一：使用 GitHub Actions（推荐）

1. 确保仓库中已有 `.github/workflows/deploy.yml` 文件（已自动创建）
2. 在 GitHub 仓库设置中：
   - 进入 **Settings** > **Pages**
   - 在 **Source** 中选择 **GitHub Actions**
3. 推送代码到 `main` 或 `master` 分支，GitHub Actions 会自动构建并部署

#### 方法二：手动部署

1. 构建项目（使用 GitHub Pages 专用命令）：

   ```bash
   npm run build:gh-pages
   ```

   或者手动设置 base 路径：

   ```bash
   VITE_BASE_PATH=/Christmas_Tree-3d/ npm run build:deploy
   ```

   **注意：** 将 `/Christmas_Tree-3d/` 替换为你的实际仓库名

2. 在 GitHub 仓库设置中：

   - 进入 **Settings** > **Pages**
   - 在 **Source** 中选择 **Deploy from a branch**
   - 选择分支（如 `main`）和文件夹 `/dist`
   - 点击 **Save**

3. 将 `dist` 目录的内容推送到 `gh-pages` 分支，或使用 GitHub Desktop 上传

**重要提示：**

- GitHub Pages 的 URL 格式是 `https://username.github.io/repository-name/`
- 确保 `vite.config.ts` 中的 base 路径与仓库名匹配
- 如果仓库名是 `Christmas_Tree-3d`，base 路径应该是 `/Christmas_Tree-3d/`

### Vercel / Netlify

1. 连接你的 Git 仓库
2. 构建命令：`npm run build:deploy`
3. 输出目录：`dist`
4. 部署即可

### 其他静态托管服务

直接将 `dist` 目录的内容上传到你的静态文件服务器即可。

## 🛠️ 技术栈

- **React** 18.2.0 - UI 框架
- **Three.js** 0.160.0 - 3D 图形库
- **@react-three/fiber** - React Three.js 渲染器
- **@react-three/drei** - Three.js 工具库
- **Tailwind CSS** 4.1.18 - 样式框架
- **Vite** 6.2.0 - 构建工具
- **TypeScript** - 类型支持

## 📝 项目结构

```
├── components/          # React 组件
│   ├── Experience.tsx  # 3D 场景主组件
│   ├── GestureController.tsx  # 手势控制
│   └── ...
├── dist/               # 构建输出目录
├── public/             # 静态资源
├── src/                # 源代码
├── vite.config.ts      # Vite 配置
├── tailwind.config.js  # Tailwind 配置
└── package.json        # 项目配置
```

## ⚠️ 常见问题

### Q: 直接打开 index.html 遇到 CORS 错误？

**A:** 这是浏览器的安全限制。请使用启动脚本启动本地服务器，或部署到线上服务器。

### Q: 构建失败，提示 Node.js 版本不兼容？

**A:** 请确保使用 Node.js 18+ 版本。可以使用 `nvm` 管理 Node.js 版本：

```bash
nvm install 18
nvm use 18
```

### Q: 摄像头无法使用？

**A:** 确保浏览器已授予摄像头权限，或使用鼠标进行交互。

### Q: GitHub Pages 部署后访问页面显示 404 或资源加载失败？

**A:** 这通常是因为 base 路径配置不正确。解决方法：

1. **使用正确的构建命令：**

   ```bash
   npm run build:gh-pages
   ```

   这会自动设置正确的 base 路径 `/Christmas_Tree-3d/`

2. **手动设置 base 路径：**
   如果仓库名不是 `Christmas_Tree-3d`，需要修改：

   - `package.json` 中的 `build:gh-pages` 脚本
   - `.github/workflows/deploy.yml` 中的 `VITE_BASE_PATH` 环境变量

   将 `/Christmas_Tree-3d/` 替换为你的实际仓库名（格式：`/repository-name/`）

3. **检查构建后的文件：**
   构建完成后，检查 `dist/index.html` 中的资源路径：

   - 应该是 `./assets/...` 或 `/Christmas_Tree-3d/assets/...`
   - 不应该是 `/assets/...`（绝对路径在子路径下会失败）

4. **重新部署：**
   使用正确的构建命令重新构建，然后推送到 GitHub

## 📄 许可证

本项目为私有项目。

## 🙏 致谢

感谢所有开源项目的贡献者。
